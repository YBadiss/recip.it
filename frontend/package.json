{
  "name": "recipe-monorepo",
  "version": "1.0.0",
  "description": "Monorepo for Recipe application",
  "private": true,
  "workspaces": [
    "core",
    "web"
  ],
  "scripts": {
    "build": "tsc --build web",
    "dev": "cd web && npm run dev"
  },
  "devDependencies": {
    "typescript": "^5.0.2"
  }
} 